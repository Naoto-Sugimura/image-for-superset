---
name: tag-account-creation
description: >
  ITHMプロジェクトのJiraチケットを検索し、アカウント作成依頼と判定されるチケットに
  問題タイプ「#アカウント作成」タグを付与するスキル。
  「アカウントにタグをつけて」「ITHMのアカウント作成チケットをタグ付けして」
  「問題タイプを整理したい」「#アカウント作成 タグを適用して」と言われたときに使う。
version: 1.0.0
---

# ITHMチケット：#アカウント作成 自動タグ付けスキル

## 概要

ITHMプロジェクトのJiraチケットを対象に、要約・説明テキストを読み取って
「新規アカウント作成依頼」か否かを判定し、該当するチケットに
`customfield_10117`（問題タイプ）= `["#アカウント作成"]` を書き込む。

**Jira接続情報**
- cloudId: `9c4e109c-2d6c-426e-a76c-071907516789`
- 問題タイプフィールド: `customfield_10117`（文字列配列）
- 更新値: `["#アカウント作成"]`

---

## 処理フロー

### Step 1. 対象チケットを取得する

引数でチケットキーが指定された場合はそのまま使う。
指定がない場合は以下のJQLで未タグチケットを取得する（最大50件ずつ処理）。

```jql
project = ITHM
  AND "問題タイプ[Labels]" in ("#リクエスト", "#TBD", "#アカウント")
  AND created >= -90d
ORDER BY created DESC
```

`mcp__atlassian__searchJiraIssuesUsingJql` を使い、`fields` に
`["summary", "description", "customfield_10117", "customfield_11100"]` を指定して取得する。

### Step 2. 各チケットを判定する

取得したチケットの **要約（summary）と説明（description）の先頭500文字** を読み、
以下の判定基準に従って分類する。

#### #アカウント作成 と判定する条件

以下のいずれかが要約または説明に含まれる場合は **#アカウント作成**：

| 判定キーワード | 例 |
|---|---|
| 「アカウント作成」「アカウント申請」「アカウント発行」 | 「Slackアカウント作成依頼」 |
| 「ID発行」「アカウント追加」「新規登録」 | 「GitHub ID発行依頼（山田）」 |
| 「ユーザー作成」「ユーザー登録」「ユーザー追加」 | 「Jiraユーザー登録」 |
| 「アカウント作成・権限付与」（新規が主旨の場合） | 「入社者のアカウント作成」 |
| 「〇〇へのアクセス申請」かつ新規ユーザーであることが明確 | 「新入社員の〇〇アクセス申請」 |

#### #権限付与・変更 と判定する条件（#アカウント作成 ではない）

以下に該当する場合は **スキップ**（別タグが適切）：

| 条件 | 例 |
|---|---|
| 「権限付与」「権限変更」「権限追加」が主旨で既存アカウントへの操作 | 「既存アカウントの管理者権限付与」 |
| 「ロール変更」「アクセス権変更」 | 「閲覧権限から編集権限に変更」 |
| アカウントが既に存在し、権限のみ変更する旨が明記されている | 「〇〇さんの権限をReadからWriteに」 |

#### 判定不能・スキップ条件

- 説明が空または極端に短い（10文字未満）
- アカウント作成とも権限変更とも読み取れない
- すでに `#アカウント作成` タグが付いている

### Step 3. タグを更新する

判定結果が **#アカウント作成** のチケットに対して、
`mcp__atlassian__editJiraIssue` で以下のフィールドを更新する。

```json
{
  "customfield_10117": ["#アカウント作成"]
}
```

**注意事項：**
- 既存の `customfield_10117` の値は上書きされる。既存タグが `["#自動化"]` の場合は触らない。
- `#自動化` タグはAIが自動付与するタグのため、このスキルでは絶対に変更しない。
- サブタスク（issuetype.subtask = true）はスキップする。

### Step 4. 結果をレポートする

処理後に以下の形式でサマリーを出力する：

```
## タグ付け結果サマリー

処理対象: XX件
├─ #アカウント作成 を付与: XX件
├─ スキップ（#権限付与・変更 相当）: XX件
├─ スキップ（判定不能）: XX件
└─ スキップ（既にタグあり）: XX件

### 付与したチケット
| チケットキー | 要約 | 判定根拠 |
|---|---|---|
| ITHM-XXXXX | 〇〇アカウント作成依頼 | 「アカウント作成」を含む |
...

### スキップしたチケット（要確認）
| チケットキー | 要約 | スキップ理由 |
|---|---|---|
| ITHM-XXXXX | 〇〇 | 権限変更が主旨のため |
...
```

---

## 使用例

### 例1: 特定チケットに1件タグ付け

```
ITHM-22848 にアカウント作成タグをつけて
```

→ ITHM-22848 の要約/説明を読み、#アカウント作成 と判定されれば更新する。

### 例2: まとめてタグ付け（直近90日）

```
ITHMの未タグチケットにアカウント作成タグをつけて
```

→ JQLで対象チケットを取得し、最大50件を一括判定・更新する。

### 例3: 特定日付範囲を指定

```
今月のITHMチケットにアカウント作成タグをつけて
```

→ JQLの `created` 条件を当月に絞って実行する。

---

## 注意・制約

- このスキルは `customfield_10117`（問題タイプ）**のみ** を更新する。他のフィールドは変更しない。
- 1回の実行で処理するチケット数は最大 **50件** に制限する（過多な変更を防ぐため）。
  50件を超える場合はユーザーに確認してから続きを実行する。
- 判定に迷うチケットは「スキップ（要確認）」リストに入れ、ユーザーが目視確認できるようにする。
- 変更前に「XX件に付与します、よろしいですか？」と確認を取る。
